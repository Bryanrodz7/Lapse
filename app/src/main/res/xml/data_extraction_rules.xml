<?xml version="1.0" encoding="utf-8"?>
<!--
  API 31+.

  Cloud backup is switched off wholesale. Auto Backup would otherwise copy filesDir — photos
  included — to Google's servers, which contradicts the app's one promise: nothing leaves the
  device. The Settings screen states that plainly, so it has to be true.

  Device-to-device transfer is kept: that is a local copy to the user's new phone, not an
  upload, and losing every tracked date when someone changes handset would be a poor trade.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="file" path="." />
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
